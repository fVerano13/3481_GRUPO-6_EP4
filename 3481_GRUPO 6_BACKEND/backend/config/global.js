//este archivo es para crear las variables globales
module.exports = {
//acá se indica el JSON que va a exportar
    port: 3000,
    //se especifica el puerto de escucha para Node
    secret: 'mysecrettext'
    //se especifica la palabra secreta con la que se hará el JSON Web Token
}